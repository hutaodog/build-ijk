name: Build IJKPlayer AAR

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install NDK r21e
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r21e-linux-x86_64.zip -O /tmp/ndk.zip
          sudo unzip -q /tmp/ndk.zip -d /opt/
          echo "ANDROID_NDK=/opt/android-ndk-r21e" >> $GITHUB_ENV

      - name: Clone IJKPlayer
        run: |
          git clone https://github.com/bilibili/ijkplayer.git source
          cd source
          git checkout k0.8.8

      - name: Init submodules
        working-directory: source
        run: bash init-android.sh

      - name: Init config
        working-directory: source
        run: bash init-config.sh

      - name: Fix module.sh
        working-directory: source
        run: echo '. "$FF_BUILD_ROOT/../../config/module-lite.sh"' > config/module.sh

      - name: Compile FFmpeg arm64
        working-directory: source/android/contrib
        run: bash compile-ffmpeg.sh arm64

      - name: Fix build scripts
        working-directory: source
        run: |
          find . -name "Application.mk" -exec sed -i 's/APP_STL := stlport_static/APP_STL := c++_static/g' {} \;
          find . -name "Android.mk" -exec sed -i 's/LOCAL_STATIC_LIBRARIES := android-ndk-profiler ijksoundtouch/LOCAL_STATIC_LIBRARIES := ijksoundtouch/g' {} \;

      - name: Compile IJKPlayer arm64
        working-directory: source/android/ijkplayer/ijkplayer-arm64/src/main/jni
        run: |
          $ANDROID_NDK/ndk-build \
            NDK_PROJECT_PATH=. \
            NDK_APPLICATION_MK=Application.mk \
            APP_BUILD_SCRIPT=Android.mk \
            -j$(nproc)

      - name: Collect outputs
        run: |
          mkdir -p output/libs/arm64-v8a
          mkdir -p output/java
          cp source/android/ijkplayer/ijkplayer-arm64/src/main/libs/arm64-v8a/*.so output/libs/arm64-v8a/ || true
          cp -r source/android/ijkplayer/ijkplayer-java/src/main/java/com output/java/ || true

      - name: Upload outputs
        uses: actions/upload-artifact@v4
        with:
          name: ijkplayer-arm64
          path: output/
